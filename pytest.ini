[pytest]
testpaths = tests
markers =
    needs_db: 需要連線 PostgreSQL 測試庫（kis_pytest / kis_pytest_collector）才能執行的測試；
        PostgreSQL 不可連線時會透過 fixture 相依（clean_db／pg_client／api_client 等）自動 skip，
        並附上清楚原因。純邏輯測試不加這個 marker，永遠會執行。
